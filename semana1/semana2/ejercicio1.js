const { ask } = require("../helpers/input");

function diffDays(a,b){
return edadEnDias;

  const  diffDays= diff (new Date(), futureDate)
  console.log("La fecha es: ", birthDate);

}
async function main() {
    const futureDate = newDate (await ask("Hasta cuand quieres imprimir la fecha?") `${birthDateYear}-${birthDateMonth}-${birthDateDay}`
)
let diff = diffDays(newDate (), futureDate)
console.log ("Diferencia: ", diff);


while(diff > 0){
    diff = diff -1;
    console.log("valor actual", diff);
}

 const personas = []
console.log("Recorrer con for");
fro (let i = personas.length - 1;  i >= 0; i--){
    saludar(personas[i]);
    console.log("Recorrer con foreach");
    personas.forEach((persona) => saludar (persona));
})

}

main();
